https://sigmadream.github.io/practice-ml/

RandomForestClassfier

데이터가 적을 때 선택할 수 있는 알고리즘: 랜덤 포레스트, 커널 소프트 벡터

1. 수렴 
2. 

SVM
01. 전처리 되어야
02. 

비지도학습

지도학습 
> 데이터 쌍이 필요하다 (x,y)
> 트리 -> 특징 추출(중요도)
> 랜덤 포레스트 -> 결정트리를 여러개 사용 -> 분류 & 회귀
=> 전처리 무관

랜덤 포레스트 연습해봐야 함!

> SVM + 전처리, 파이프

지도학습 세계관: y = ax + b

비지도학습
> 데이터가 x 만 있다
> 군집: 비슷한 애들끼리 묶는 것(X 의 특성을 공유)
  -> 교수님 자료 예제 한번 해보세요
  
전처리
> 클리닝 -> NA 없애기
	    -> 문자열 정리: 판다스
		-> 문자를 숫자로 바꾸기 [선형 모델의 단점]
> 보정: 보간

426개에 특징 30개

인터넷 안 열어주는 면접장
> 면접관들과 이야기하며 해결해야
> 경험치를 쌓는 시간: 별로 -> 가고싶은 -> 가능성 희박한 순으로 면접 배치

scaler:범위가 중요
비율은 유지한채 축약

fit: 데이터에 적용한다

비율로써의 경향성은 사는데 원데이터가 사라진다.

MinMaxScaler: SVM 에서 많이 쓴다

정규화 -> StandardScaler
1. DB: 종속 f(x) 해결
2. 수학

정규화, 표준정규분포
-> 언제 쓰는가?
	일본의 급여, 한국의 급여, 미국의 급여 비교하고 싶을때?
	서로 단위가 다른 세 데이터가 주어졌을 때
		-> 전처리 필요: 단위 통일
-> 평균, 분산 알때 구한다

StandardScaler
> min, max 제거
> 중위값

사분위는 사용하지 않는다.
알아야할 것은 MinMaxScaler, StandardScaler

K-NN: StandardScaler 사용하면 안된다.
-> MinMaxScaler 사용: 제일 큰애와 작은애 제끼기
-> 보고서에 작성: 제외된 데이터

데이터 -> 지도 -> 선형 -> 랜덤 -> 분석

      -> 비지도 -> t-SNE(데이터를 주면 알아서 분류) -> 분석
	           -> 군집(k-평균: 데이터 조작해야)
			   -> PCA 주의!(주요 특성을 추출하는 용도로 사용: 랜덤 트리와 겹친다.): 특성을  몰라서 대충 때렸다는 의미...쓰긴쓰되 주의해서 써야함

데이터: 클리닝, 보간 (판다스)

수익, 매출, 출하량 전부 다 비지도

t-SNE, 군집으로 낸 주요특성이 정말?: PCA 사용

픽셀: 빛나는 소자
NxNxN 정방행렬, 세장이 겹쳐짐
MNIST 숫자 예측: 

t-SNE 쓰는 이유: PCA 비해 해석력이 월등
> transform 함수가 없어 fit_transform 를 사용해야
> split 없다.
  fit_transform(digits.data)
> 특성 데이터를 꺼내올 때 리스트로 되어 있는데 min, max 사용
plt.xlim(digits_tsne[:, 0].min(), digits_tsne[:, 0].max() + 1)
plt.ylim(digits_tsne[:, 1].min(), digits_tsne[:, 1].max() + 1)

digits_tsne[i, 0],
digits_tsne[i, 1],

i: 인덱스
abcd -> abcd

그래프 읽는 연습 필요하다: 표로 만들 수 있어야 한다: 그래야 설명할 수 있다.

리포팅 보고서 쓰는 연습 해야한다.
반 페이지 보고서: 제목, 한줄 내용 정리, 결과

K군집: 초기화가 중요 -> t-SNE 으로 특성 조정해서 초기화가

sklearn.decompose PCA
sklearn.manifold TSNE
sklearn.cluster KMeans

k-means 부터 먼저 쓰고 데이터 정제 후 t-SNE 쓰기

코테: 연결리스트 짜봐, 균형트리 짜봐
쉬운데 안해봐서 틀린다.
프로그래머스 0단계부터 차근차근 다 풀어
쉬운 것부터 먼저, 안 그러면 어려운 것도 못함

주성분 분석: 직교 데이터를 기반으로 떼내기
675차원 짜리 데이터
수리적으로 직교하는 애들

TSNE 하고 군집으로 체크하고 PCA 사용
군집에서 막히면 앞으로 가서 데이터를 좀더 정제한다.

이미지 분류: CNN
머신러닝: 수치형 정형 데이터

2 지도
3 비지도: 전처리 필요
-> 원데이터의 정확도 문제

4 특성공학 -> 2,3 좀더 잘 쓸 수 있다.

5 평가 [중요!]

-> 자동화를 잘 해야한다

6 자동화

7 텍스트

2,3장 노트는 한번씩 봐야 한다
시간 없으면 랜덤포레스트, 결정트리, SVM 
그거 하고 TSNE, KMEANS, PCA 정도는 봐야 한다
	
